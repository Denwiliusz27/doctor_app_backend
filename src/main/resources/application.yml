server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/doctor_app
    username: root
    password: haslo
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    show-sql: false
  properties:
    hibernate:
      jdbc:
        lob:
          non_contextual_creation: true
